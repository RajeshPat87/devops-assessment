apiVersion: v1
# Non-sensitive configuration only. Anything secret lives in db-credentials.
data:
  APP_ENV: "lab"
  DB_HOST: "postgres"
  DB_NAME: "appdb"
  DB_PORT: "5432"
  GREETING: "Hello World from Kubernetes"
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: hello-web
    app.kubernetes.io/part-of: demo-app
  name: web-config
  namespace: demo-app
