# Lab-only Secret checked into git for reproducibility.
# In a real environment this file would NOT exist in the repo. Options,
# in order of preference: External Secrets Operator backed by Azure Key
# Vault / AWS Secrets Manager, Sealed Secrets, or SOPS-encrypted YAML.
# See DESIGN.md > Trade-offs.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: demo-app
  labels:
    app.kubernetes.io/part-of: demo-app
type: Opaque
stringData:
  DB_USER: "appuser"
  DB_PASSWORD: "S3cure-Lab-Passw0rd"
  POSTGRES_DB: "appdb"
